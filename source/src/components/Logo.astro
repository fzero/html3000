<logo>
  <logo-inner> <em>HTML</em>3000 </logo-inner>
</logo>

<style>
  logo {
    font-family: var(--heading-font);
    font-weight: bold;
    display: block;
    font-size: 7.5vw;
    color: var(--pico-primary);
    margin-bottom: 1rem;
  }

  logo-inner {
    display: inline;
    border: 1px solid var(--pico-primary);
  }

  logo-inner em {
    font-style: normal;
    background-color: var(--pico-primary);
    color: var(--pico-background-color);
  }
</style>
